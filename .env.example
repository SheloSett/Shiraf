# Plantilla de variables de entorno. Copiala a .env y completá los valores:
#
#   cp .env.example .env
#
# Los valores están en el panel de Supabase → Project Settings → API.
# El .env real NO va al repo (está en .gitignore).
#
# Por qué cada variable aparece dos veces: las VITE_* las hornea Vite en el
# bundle del navegador en tiempo de build; las que no tienen prefijo las lee
# process.env en runtime, del lado servidor (SSR y middleware de auth). Ver
# src/integrations/supabase/client.ts, que primero prueba import.meta.env y
# después cae a process.env.

SUPABASE_PROJECT_ID="tu-project-id"
SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
SUPABASE_URL="https://tu-project-id.supabase.co"
VITE_SUPABASE_PROJECT_ID="tu-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_..."
VITE_SUPABASE_URL="https://tu-project-id.supabase.co"

# ⚠️ SECRETA — bypasea toda la RLS. NUNCA la pongas en una variable VITE_*
# (terminaría en el bundle que baja el navegador) ni la commitees.
#
# Todavía no hace falta: hoy no hay ningún createServerFn en src/. La espera
# src/integrations/supabase/client.server.ts para cuando se implementen las
# notificaciones y la carga de turnos por parte del admin. En producción va en
# el `environment` del docker-compose.yml, no acá.
# SUPABASE_SERVICE_ROLE_KEY="sb_secret_..."
